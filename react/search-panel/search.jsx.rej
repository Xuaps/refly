diff a/react/search-panel/search.jsx b/react/search-panel/search.jsx	(rejected hunks)
@@ -11,7 +11,7 @@ var NOT_FOUND = 'Reference not found!', LOADING = 'Loading...';
 
 module.exports = React.createClass({
     getInitialState: function() {
-        return {data: {results: [], reached_end:true, docset:null}, message:'', current_index: -1};
+        return {data: {results: [], reached_end:true, docset:null}, message:'', current_index: 0};
     },
     
     getDefaultProps: function() {
@@ -88,12 +88,7 @@ module.exports = React.createClass({
     },
 
     componentWillUnmount: function(){
-        Mousetrap.unbind('left');
-        Mousetrap.unbind('down');
-        Mousetrap.unbind('up'); 
-        Mousetrap.unbind('enter');
-        Mousetrap.unbind('tab');
-        Mousetrap.unbind('backspace');
+        this.unbindKeys();
         window.removeEventListener('resize', this.calculateHeight, false);
         this.unsubscribe();
     },
@@ -149,7 +144,13 @@ module.exports = React.createClass({
         this.props.onKeyUpEvent({target:{value: ''}});
         this.cleanResults();
     },
-    
+    unbindKeys: function(){
+        this.mousetrap.unbind('tab');
+        this.mousetrap.unbind('backspace');
+        this.mousetrap.unbind(['enter', 'right']);
+        this.mousetrap.unbind('down');
+        this.mousetrap.unbind('up'); 
+    },
     bindKeys: function(){
         this.mousetrap.bind('tab',function(e){
             e.preventDefault();
@@ -166,7 +167,7 @@ module.exports = React.createClass({
             e.preventDefault();
             this.goUp();
         }.bind(this));
-        this.mousetrap.bind('enter',function(e){
+        this.mousetrap.bind(['enter', 'right'],function(e){
             e.preventDefault();
             this.selectResult();
         }.bind(this));
@@ -196,6 +197,7 @@ module.exports = React.createClass({
     },
 
     cleanResults: function(){
+        this.unbindKeys();
         this.setState({data:{results:[], reached_end:true, docset: this.state.data.docset}, message: '', current_index: -1});
     },
 
@@ -206,29 +208,31 @@ module.exports = React.createClass({
 	},
 
     goUp: function(){
-        if(this.state.data.results.length>0){
-            if(this.state.current_index<1)
-                var current_index = 0;
-            else
+        if(this.state.data.results.length>0 && this.props.searchVisible){
+            if(this.state.current_index>1){
                 var current_index = this.state.current_index - 1;
-            this.setState({current_index: current_index});
-            this.updateScroll();
+                this.setState({current_index: current_index});
+                this.updateScroll();
+            }
+        }else{
+            this.unbindKeys();
         }
     },
 
     goDown: function(){
-        if(this.state.data.results.length>0){
-            if(this.state.current_index>=this.state.data.results.length)
-                var current_index = this.state.data.results.length-1;
-            else
+        if(this.state.data.results.length>0 && this.props.searchVisible){
+            if(this.state.current_index<this.state.data.results.length-1){
                 var current_index = this.state.current_index + 1;
-            this.setState({current_index: current_index});
-            this.updateScroll();
+                this.setState({current_index: current_index});
+                this.updateScroll();
+            }
+        }else{
+            this.unbindKeys();
         }
     },
 
     selectResult: function(){
-        if(this.state.data.results[this.state.current_index]!= undefined){
+        if(this.state.data.results[this.state.current_index]!= undefined && this.props.searchVisible){
             var uri = this.state.data.results[this.state.current_index].uri;
             this.loadReference(uri);
         }
